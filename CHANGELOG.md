# 變更紀錄

版本號顯示在首頁大標右側。Service Worker 的 `VERSION` 是快取版本，與功能版本無關 —— 每次改動靜態資源都要遞增，否則使用者會拿到舊快取。

---

## V0.9

規格第 11 節的八個步驟全部完成，另加三輪 UI/UX 演出與效能優化。

### 功能
- 拆檔重構：單一 HTML 拆成原生 ES modules，偵測演算法逐幀行為與原版一致
- 安全規則：Firestore 與 RTDB，**49 項行為驗證通過**（emulator）
- Google 登入與自訂暱稱（不沿用 Google 真名、不儲存 email）
- 四碼房號對戰：建房/加入/分享、presence、30 秒斷線寬限、伺服器時鐘同步
- 判定標準：標準 / 寬鬆 / 房主的 / 各自 —— 雙方套用同一組門檻，基準各自校正
- 對戰時長加入「無限」（由任一方長按 ✕ 結束）
- 校正結果存本機自動帶入，換鏡頭或超過一天會提示重校
- 戰績：勝敗統計與對戰紀錄清單
- PWA：manifest、圖示、Service Worker

### 效能與省電
- **修掉無效的影格去重**：`ts===lastTs` 永不成立，導致同一 video frame 推論兩次
- **推論閘門**：只在校正/倒數/計數時推論，首頁、大廳、等待室、結算完全停止
- **首頁不啟用相機**，改成按下開始才開
- **進背景停止供幀，超過 30 秒完整釋放相機**
- **比賽進行中一律豁免**上述省電處理 —— 回來時相機重啟會漏算，計數準確度優先
- `#gaugeFill` 由 `height` 改 `transform: scaleY()`（原本每幀觸發 reflow）
- 測試模式新增推論/幀時間的 p50/p95 與長幀計數

### 演出
- 面板轉場（`clip-path` 抹入 200ms）
- 倒數 3-2-1-GO 放大收縮，GO 配全螢幕閃光
- 對手即時比分：字級 62px → 13vh、跳動 pulse、落後時脈動（規格第 7 節指名強化項）
- 勝負以**形態**區分而非顏色：勝=實心反白、敗=細框、和=虛線；勝利全螢幕反相一次
- 結算數字從 0 滾動到最終值
- 骨架顏色隨下壓深度由冷青燒到訊號橘（零成本，同時是功能回饋）
- 深度計升級為完整刻度尺，含上下兩條判定門檻線
- 按鈕按壓回彈、解鎖脈衝；中止鈕改長按 450ms（避免力竭誤觸）
- 掃描線母題（等待室與校正取樣期，**絕不進計數畫面**）
- 色彩紀律：橘色原本背八種語意，收斂成「每畫面最多一處」
- 各面板背景不再共用同一顆漸層
- 音效層次：深度驅動的漸強嗡鳴、每 5/10 下的和聲、對手加分的低音撥弦、勝負分音

### 修正
- 房主校正失敗會讓對手永遠卡在等待室（`startAt` 永不寫入）
- 對戰時鐘寫在 `onFrame` 內，相機無影像時倒數完全不動且不會結算
- 手動計數綁在 `#count` 上，但它有 `pointer-events:none`，點擊永遠穿透
- 回到前景時被接回一場早已結算的比賽（新增死房間判斷與清理）
- 「再來一場」沒有離開舊房間
- iPhone SE（375×667）首頁溢出 145px 且無法捲動，底部按鈕按不到
- Service Worker 在開發時供應舊檔案（現在 localhost 完全不介入）
- 對戰勝負在畫面上長得一樣（判定文字顏色寫死橘色）
- 斷線但缺 `leftAt` 時會把只是瞬斷的人踢掉
- `rollNumber` 在 rAF 不觸發的環境會永遠停在 0

### 命名
- REP ROOM → **PUSH ROOM**（與 repo、Firebase 專案一致）
- icon 改為 PR 字標
- localStorage key `reproom.*` → `pushroom.*`，並自動搬移舊資料
- `docs/REPROOM_BUILD_SPEC.md` → `docs/PUSHROOM_BUILD_SPEC.md`

### 尚未驗證
- 兩台實體裝置的多場對戰
- 長時間使用的熱節流表現

---

## V0.1

原始單檔版（`docs/pushup-arena-mvp.html`），實機驗證通過的單機計數器：
MediaPipe 姿勢偵測、自動訊號選擇、兩段式校正、Web Audio 音效、計時結算、測試模式、Wake Lock。
